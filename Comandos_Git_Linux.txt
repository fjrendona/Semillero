**********GIT****************************************

--Iniciar proyecto de Git

git init

--Validar status git
git status

-- Subir cambios en la plataforma

git commit -m "Comentarios"

--Conectar a un servidor 

ssh user@direccionIP -p 22

****LINUX********************************************

--Comando para crear un archivo
touch (nombre del archivo a crear)

--Comando para cambiar nombre de un archivo
mv (nombre de archivo actual) (nuevo nombre de archivo)

--Comando para devolverse a la ultima ruta donde se estuvo trabajando

cd-

--Comando para visualizar directorio actual

pwd

--Comando para agregar a un origen
git remote add origin https://github.com/fjrendona/Semillero.git (Enlace de Github)

--Comando para subir los archivos
git push origin main
git pull origin main 
git pull origin main --allow-unrelated-histories (En caso de error en Merge)


Servidores WEB y de Apps
-Apache
-Tomcat
-IIS
-Jboss
-GlassFish
-Nginx

NPM (Node Platform Management)




